{
  "name": "git-agent-proxy",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "wrangler": "^3"
  },
  "scripts": {
    "dev": "ln -sf ../.env .dev.vars && wrangler dev",
    "deploy": "wrangler deploy"
  }
}
