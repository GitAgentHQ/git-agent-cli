hook:
    - conventional
scopes:
    - name: app
      description: application logic and services in application/
    - name: cli
      description: command-line commands and flags in cmd/
    - name: domain
      description: business domain interfaces and entities in domain/
    - name: hooks
      description: git hook scripts and executor logic in hooks/
    - name: infra
      description: infrastructure adapters and integration in infrastructure/
    - name: pkg
      description: utility packages in pkg/
    - name: skills
      description: skill definitions and documentation for CLI usage in skills/
    - name: e2e
      description: end-to-end workflow tests in e2e/
